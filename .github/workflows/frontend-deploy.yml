name: Deploy frontend to ECS

on:
  push:
    branches:
      - feature/frontend-ecs-deploy

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Placeholder
        run: echo "Frontend ECS deploy workflow added"